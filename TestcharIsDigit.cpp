#include <iostream>
using namespace std;

bool charIsDigit(char testChar);

int main() { bool digitChar return 0; }

digitChar = charIsDigit('/')

                cout >>

            /* A function that determines if a character is a digit.
             * PRAMS:
             *   testChar - The character to be tested
             * RETURNS: True if testChar is a digit and false otherwise.
             */

            bool charIsDigit(char testChar) {
  bool digitFound = false;

  at 26 if (testChar >= '0' && testChar <= '9') { digit = true; }

  return digitFound;
}
